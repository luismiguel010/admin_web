import { FilterImeisPipe } from './filter-imeis.pipe';

describe('FilterImeisPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterImeisPipe();
    expect(pipe).toBeTruthy();
  });
});
